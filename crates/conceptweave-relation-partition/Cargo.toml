[package]
name = "conceptweave-relation-partition"
version.workspace = true
edition.workspace = true
rust-version.workspace = true
repository.workspace = true
license.workspace = true
description = "Immutable PostgreSQL declarative-partition evidence for ConceptWeave"

[lib]
path = "src/lib.rs"

[dependencies]
conceptweave-observation = { path = "../conceptweave-observation" }
sha2 = "0.10.9"

[dev-dependencies]
conceptweave-source-port = { path = "../conceptweave-source-port" }
